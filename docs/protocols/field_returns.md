# Field Returns

## Purpose

Field returns support simple XLSForm integration.

## Example

```text
value=73
module_id=calibrated_scale
success=true
```

## Guidelines

- use stable field names
- document every returned field
- avoid changing field meaning between versions
- use JSON for complex structures
